# 📂 Структура Проекта

## Обзор файлов

```
parking_bot/
├── main.py                 # 🚀 Точка входа в приложение
├── config.py               # ⚙️ Конфигурация и настройки
├── database.py             # 🗄️ Работа с базой данных
├── keyboards.py            # ⌨️ Клавиатуры и кнопки
├── utils.py                # 🛠️ Вспомогательные функции
├── user_handlers.py        # 👤 Обработчики пользователей
├── admin_handlers.py       # 👑 Обработчики администраторов
│
├── requirements.txt        # 📦 Зависимости проекта
├── .env                    # 🔐 Переменные окружения (токен)
├── .gitignore              # 🚫 Игнорируемые файлы
│
├── README.md               # 📖 Основная документация
├── QUICKSTART.md           # 🚀 Быстрый старт
├── TESTING.md              # 🧪 Руководство по тестированию
├── DEVELOPERS.md           # 👨‍💻 Руководство для разработчиков
└── PROJECT_STRUCTURE.md    # 📂 Этот файл
```

## Подробное описание файлов

### 🚀 main.py (96 строк)
**Назначение:** Главный файл приложения, точка входа

**Основные функции:**
- Инициализация бота и диспетчера
- Настройка логирования
- Регистрация обработчиков (роутеров)
- Установка команд бота
- Запуск polling

**Ключевые компоненты:**
```python
async def main()           # Главная функция
async def on_startup()     # Действия при запуске
async def on_shutdown()    # Действия при остановке
```

**Используется:**
- `aiogram` - фреймворк для Telegram ботов
- `asyncio` - асинхронное выполнение
- `logging` - логирование

---

### ⚙️ config.py (40 строк)
**Назначение:** Конфигурация приложения

**Содержит:**
- Токен бота (из .env)
- Пароль администратора
- Путь к базе данных
- Настройки пагинации
- Список банков
- Константы ролей и статусов

**Основные переменные:**
```python
BOT_TOKEN                  # Токен от BotFather
ADMIN_PASSWORD            # Пароль для админки
DATABASE_PATH             # Путь к БД
BANKS_LIST                # Список банков
ROLE_CUSTOMER/SUPPLIER    # Роли пользователей
STATUS_PENDING/CONFIRMED  # Статусы бронирований
```

---

### 🗄️ database.py (522 строки)
**Назначение:** Работа с базой данных SQLite

**Основные разделы:**

1. **Инициализация:**
   - `init_db()` - создание таблиц

2. **Пользователи (10 методов):**
   - `add_user()` - добавление
   - `get_user_by_telegram_id()` - получение
   - `update_user_role()` - обновление роли
   - `get_all_users()` - список с пагинацией
   - `block_user()` / `unblock_user()` - блокировка

3. **Парковочные места (7 методов):**
   - `add_parking_spot()` - добавление
   - `get_spots_by_supplier()` - места поставщика
   - `update_spot_price()` - изменение цены
   - `toggle_spot_visibility()` - скрыть/показать

4. **Доступность (4 метода):**
   - `add_availability()` - добавить период
   - `get_available_slots()` - свободные слоты
   - `check_slot_availability()` - проверка
   - `book_slot()` - бронирование слота

5. **Бронирования (6 методов):**
   - `create_booking()` - создание
   - `get_user_bookings()` - бронирования пользователя
   - `update_booking_status()` - обновление статуса
   - `get_supplier_bookings()` - бронирования поставщика

6. **Уведомления (3 метода):**
   - `add_notification_request()` - добавление
   - `get_active_notifications()` - активные
   - `deactivate_notification()` - деактивация

7. **Админские сессии (3 метода):**
   - `create_admin_session()` - создание
   - `check_admin_session()` - проверка
   - `is_admin()` - проверка прав

8. **Статистика (1 метод):**
   - `get_statistics()` - общая статистика

**Таблицы БД:**
```sql
users               # Пользователи
parking_spots       # Парковочные места
spot_availability   # Доступность мест
bookings            # Бронирования
notifications       # Уведомления
admin_sessions      # Админские сессии
```

---

### ⌨️ keyboards.py (312 строк)
**Назначение:** Все клавиатуры и кнопки

**Типы клавиатур:**

1. **Главные меню:**
   - `get_main_menu()` - для покупателя/поставщика/админа
   - `get_cancel_button()` - кнопка отмены
   - `get_admin_menu()` - меню администратора

2. **Регистрация:**
   - `get_phone_keyboard()` - отправка телефона
   - `get_banks_keyboard()` - выбор банка
   - `get_role_selection()` - выбор роли

3. **Поставщик:**
   - `get_partial_allowed_keyboard()` - частичная аренда
   - `get_date_selection_keyboard()` - быстрый выбор даты
   - `get_spots_keyboard()` - список мест
   - `get_spot_management_keyboard()` - управление местом

4. **Покупатель:**
   - `get_available_slots_keyboard()` - доступные слоты
   - `get_bookings_keyboard()` - бронирования
   - `get_booking_actions_keyboard()` - действия с бронированием
   - `get_confirm_booking_keyboard()` - подтверждение

5. **Админ:**
   - `get_pagination_keyboard()` - пагинация
   - `get_user_actions_keyboard()` - действия с пользователем
   - `get_broadcast_confirm_keyboard()` - подтверждение рассылки

6. **Общие:**
   - `get_profile_keyboard()` - профиль

**Используемые типы:**
- `ReplyKeyboardMarkup` - основные кнопки
- `InlineKeyboardMarkup` - inline кнопки

---

### 🛠️ utils.py (292 строки)
**Назначение:** Вспомогательные функции

**Категории функций:**

1. **Валидация (5 функций):**
   - `validate_phone()` - телефон
   - `validate_card_number()` - карта
   - `validate_date()` - дата
   - `validate_time()` - время
   - `validate_price()` - цена

2. **Форматирование (8 функций):**
   - `format_phone()` - телефон
   - `mask_card_number()` - маскировка карты
   - `format_datetime()` - дата и время
   - `format_date()` - только дата
   - `format_time()` - только время
   - `escape_html()` - экранирование HTML
   - `truncate_text()` - обрезка текста

3. **Расчеты (3 функции):**
   - `calculate_hours()` - количество часов
   - `calculate_price()` - стоимость
   - `parse_datetime()` - парсинг даты/времени

4. **Статусы (2 функции):**
   - `get_status_emoji()` - emoji статуса
   - `get_status_text()` - текст статуса

5. **Временные интервалы (3 функции):**
   - `check_time_overlap()` - пересечение
   - `split_slot()` - разделение слота
   - `is_past_datetime()` - проверка прошедшего
   - `get_upcoming_dates()` - ближайшие даты

6. **Форматирование информации (3 функции):**
   - `format_booking_info()` - информация о бронировании
   - `format_spot_info()` - информация о месте
   - `format_user_info()` - информация о пользователе

---

### 👤 user_handlers.py (405 строк)
**Назначение:** Обработчики для обычных пользователей

**Основные группы состояний:**
```python
Registration      # Регистрация (4 состояния)
AddSpot          # Добавление места (8 состояний)
SearchSpot       # Поиск места (2 состояния)
BookingProcess   # Бронирование (2 состояния)
NotificationRequest  # Уведомления (3 состояния)
```

**Основные обработчики:**

1. **Регистрация (5 обработчиков):**
   - `/start` - начало
   - Ввод имени
   - Ввод телефона
   - Ввод карты
   - Выбор банка и роли

2. **Поставщик - Добавление места (10 обработчиков):**
   - Начало добавления
   - Номер места
   - Цена
   - Адрес
   - Описание
   - Частичная аренда
   - Дата доступности
   - Время начала/окончания

3. **Поставщик - Управление (3 обработчика):**
   - Просмотр мест
   - Детали места
   - Переключение видимости

4. **Покупатель - Поиск (4 обработчика):**
   - Свободные места на сегодня
   - Выбор даты
   - Просмотр слотов

5. **Покупатель - Бронирования (2 обработчика):**
   - Список бронирований
   - Детали бронирования

6. **Профиль (2 обработчика):**
   - Просмотр профиля
   - Возврат в главное меню

---

### 👑 admin_handlers.py (262 строки)
**Назначение:** Обработчики для администраторов

**Группы состояний:**
```python
AdminAuth     # Авторизация (1 состояние)
Broadcast     # Рассылка (2 состояния)
```

**Основные обработчики:**

1. **Авторизация (2 обработчика):**
   - `/admin` - вход
   - Проверка пароля

2. **Пользователи (4 обработчика):**
   - Список пользователей
   - Пагинация
   - Блокировка/разблокировка
   - Назначение админом

3. **Парковочные места (1 обработчик):**
   - Просмотр всех мест

4. **Статистика (1 обработчик):**
   - Общая статистика

5. **Рассылка (3 обработчика):**
   - Начало рассылки
   - Ввод текста
   - Подтверждение

6. **Настройки (1 обработчик):**
   - Просмотр настроек

7. **Общие (1 обработчик):**
   - Выход из админки

---

### 📦 requirements.txt (3 строки)
**Назначение:** Список зависимостей проекта

**Содержимое:**
```
aiogram==3.4.1           # Telegram Bot Framework
aiosqlite==0.19.0        # Асинхронный SQLite
python-dotenv==1.0.0     # Переменные окружения
```

**Установка:**
```bash
pip install -r requirements.txt
```

---

### 🔐 .env (3 строки)
**Назначение:** Переменные окружения

**Содержимое:**
```env
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
ADMIN_PASSWORD=qwerty123
```

**⚠️ ВАЖНО:**
- Не публикуйте этот файл с реальным токеном
- Добавлен в .gitignore
- Токен получить у @BotFather

---

### 🚫 .gitignore (31 строка)
**Назначение:** Игнорируемые файлы для Git

**Категории:**
- Python файлы (`__pycache__`, `*.pyc`)
- Виртуальное окружение (`venv/`)
- IDE файлы (`.vscode/`, `.idea/`)
- Переменные окружения (`.env`)
- База данных (`*.db`)
- Логи (`*.log`)
- OS файлы (`.DS_Store`)

---

## 📚 Документация

### 📖 README.md
**Объем:** ~300 строк

**Разделы:**
- Описание функционала
- Установка и запуск
- Структура проекта
- Использование
- Настройка
- Деплой
- Важные замечания

---

### 🚀 QUICKSTART.md
**Объем:** ~200 строк

**Разделы:**
- Быстрый старт за 5 минут
- Получение токена
- Настройка
- Первые действия
- Решение проблем
- Деплой

---

### 🧪 TESTING.md
**Объем:** ~450 строк

**Разделы:**
- Тестовые сценарии (10 штук)
- Проверка обработки ошибок
- Чек-лист функционала
- Проверка базы данных
- Метрики качества
- Известные ограничения
- Отчет о тестировании

---

### 👨‍💻 DEVELOPERS.md
**Объем:** ~500 строк

**Разделы:**
- Работа с Database API
- Примеры клавиатур
- Утилиты
- FSM (состояния)
- Логирование
- Добавление обработчиков
- Best Practices
- Безопасность
- Расширение функционала

---

### 📂 PROJECT_STRUCTURE.md
**Объем:** ~400 строк (этот файл)

**Содержание:**
- Структура проекта
- Описание каждого файла
- Статистика проекта

---

## 📊 Статистика проекта

### Кодовая база
```
Python файлы:       7 файлов
Строк кода:         ~1,889 строк
Функций/методов:    ~80
Классы:            ~10
Состояния FSM:     ~20
```

### Документация
```
Markdown файлы:     5 файлов
Строк документации: ~1,850 строк
```

### База данных
```
Таблиц:            6
Полей всего:       ~50
Индексов:          По telegram_id, spot_id, etc.
```

### Функционал
```
Роли:              3 (customer, supplier, admin)
Команды:           2 (/start, /admin)
Клавиатур:         ~20
Обработчиков:      ~40
```

## 🎯 Зависимости между файлами

```
main.py
  ├── config.py
  ├── database.py
  ├── user_handlers.py
  │   ├── database.py
  │   ├── keyboards.py
  │   ├── utils.py
  │   └── config.py
  └── admin_handlers.py
      ├── database.py
      ├── keyboards.py
      ├── utils.py
      └── config.py
```

## 💡 Рекомендации по работе

### Для новых разработчиков:
1. Начните с `QUICKSTART.md`
2. Изучите `README.md`
3. Просмотрите `database.py` для понимания структуры
4. Читайте `DEVELOPERS.md` для примеров кода
5. Используйте `TESTING.md` для тестирования

### Для модификации:
1. **Добавить новую функцию:**
   - Метод в `database.py`
   - Обработчик в `*_handlers.py`
   - Клавиатуру в `keyboards.py`
   - Утилиту в `utils.py` (если нужно)

2. **Изменить UI:**
   - Модифицируйте `keyboards.py`
   - Обновите форматирование в `utils.py`

3. **Добавить роль:**
   - Константу в `config.py`
   - Проверки в обработчиках
   - Новое меню в `keyboards.py`

---

**Проект готов к использованию и расширению! 🚀**

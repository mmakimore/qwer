# 🚀 Быстрый Старт

## За 5 минут до запуска!

### Шаг 1: Получите токен бота (2 минуты)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте: `/newbot`
4. Введите имя бота: `My Parking Bot`
5. Введите username: `my_parking_bot` (должен заканчиваться на `bot`)
6. **СКОПИРУЙТЕ ТОКЕН** (выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Настройте проект (1 минута)

1. Откройте файл `.env`
2. Вставьте ваш токен:
```
BOT_TOKEN=ВАШ_ТОКЕН_СЮДА
ADMIN_PASSWORD=qwerty123
```
3. Сохраните файл

### Шаг 3: Установите зависимости (1 минута)

```bash
pip install -r requirements.txt
```

Или если у вас несколько версий Python:
```bash
pip3 install -r requirements.txt
```

### Шаг 4: Запустите бота (1 минута)

```bash
python main.py
```

Или:
```bash
python3 main.py
```

### ✅ Готово!

Вы должны увидеть:
```
INFO - Инициализация базы данных...
INFO - База данных инициализирована
INFO - Установка команд бота...
INFO - Команды установлены
INFO - Бот запущен и готов к работе!
```

## 🎯 Первые действия

1. **Откройте вашего бота в Telegram**
2. **Отправьте:** `/start`
3. **Пройдите регистрацию:**
   - Имя: `Тест Тестов`
   - Телефон: `+7 900 123 45 67`
   - Карта: `1234567812345678`
   - Банк: `Сбербанк`
   - Роль: `Я поставщик`

4. **Добавьте первое место:**
   - Кнопка: `➕ Добавить место`
   - Номер: `A1`
   - Цена: `100`
   - Адрес: `Тестовая ул, 1`
   - Описание: `Тестовое место`
   - Частичная аренда: `Да`
   - Дата: `Сегодня`
   - Время: `09:00` - `18:00`

5. **Проверьте как покупатель:**
   - Создайте второго пользователя
   - Выберите роль: `Я покупатель`
   - Кнопка: `🏠 Свободные места`
   - Забронируйте место!

## 🔧 Если что-то не работает

### Ошибка: "BOT_TOKEN не установлен"
→ Проверьте файл `.env`, убедитесь что токен правильно вставлен

### Ошибка: "No module named 'aiogram'"
→ Установите зависимости: `pip install -r requirements.txt`

### Ошибка: "Unauthorized"
→ Токен бота неверный, получите новый у @BotFather

### Бот не отвечает
→ Убедитесь что бот запущен (смотрите консоль)

## 📱 Деплой на сервер

### BotHost.ru (рекомендуется)

1. Зарегистрируйтесь на [bothost.ru](https://bothost.ru)
2. Создайте новый проект
3. Загрузите все файлы
4. В настройках:
   - **Команда запуска:** `python main.py`
   - **Python версия:** 3.9+
   - **Переменные окружения:** добавьте `BOT_TOKEN`
5. Запустите проект

### VPS/Dedicated Server

```bash
# Установите Python 3.9+
sudo apt update
sudo apt install python3 python3-pip

# Загрузите проект
git clone YOUR_REPO
cd parking_bot

# Установите зависимости
pip3 install -r requirements.txt

# Запустите через screen (чтобы не закрывался)
screen -S parking_bot
python3 main.py

# Отключитесь от screen: Ctrl+A, затем D
# Вернуться: screen -r parking_bot
```

## 🎓 Что дальше?

1. Прочитайте [README.md](README.md) - полная документация
2. Изучите [TESTING.md](TESTING.md) - руководство по тестированию
3. Настройте бота под себя в `config.py`
4. Добавьте функционал по желанию

## 💡 Советы

- Храните токен в секрете!
- Регулярно делайте бэкап `parking_bot.db`
- Измените `ADMIN_PASSWORD` в `.env` на свой
- Для продакшена используйте PostgreSQL вместо SQLite

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи в консоли
2. Убедитесь что все зависимости установлены
3. Проверьте что токен правильный
4. Убедитесь что Python 3.9+

---

**Удачи! Ваш бот готов к работе! 🎉**
